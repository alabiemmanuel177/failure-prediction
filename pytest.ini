[pytest]
# ROS 2 installs launch_testing pytest plugins system-wide; they abort collection on
# module-level skips and are irrelevant to this repository's offline test suite.
addopts = -p no:launch_testing -p no:launch_ros -p no:launch_testing_ros
testpaths = tests
