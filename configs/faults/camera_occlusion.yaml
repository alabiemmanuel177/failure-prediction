family: camera_occlusion
status: candidate
mechanism: deterministic right-edge RGB mask with optional seeded frame dropout
severities:
  low: {mask_fraction: 0.20, dropout_probability: 0.00}
  medium: {mask_fraction: 0.50, dropout_probability: 0.05}
  high: {mask_fraction: 0.80, dropout_probability: 0.20}

